79 constant linelen 
title user text @ host sp text "(" shell text ")" endtitle
?termcols linelen min cyan line ?termcols linelen - 0 max magenta line
?battery 30 < dup if red else blue then text bat: if red else cyan then battery sp
?gitbranch if
  blue text git:
  ?gitbranch ?text "master" <> if cyan gitbranch then
  ?gitstashcount if red text "+" gitstashcount then
  sp
then
green dir nl
blue time24 sp weekday sp
cyan host sp
green shell sign sp
